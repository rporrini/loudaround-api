{
  "name": "loudaround-api",
  "version": "0.0.1",
  "scripts": {
    "start": "node src/web.js",
    "test": "npm run -s test:runner -- test/unit/*.test.js",
    "test:performance": "npm run -s test:runner -- test/performance/*.test.js",
    "test:runner": "./node_modules/.bin/mocha --reporter dot test/_fixture.js"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.1.0",
    "ws": "^1.0.0"
  },
  "dependencies": {
    "express": "^4.14.0",
    "express-ws": "^2.0.0"
  },
  "engines": {
    "node": "6.5.0"
  }
}
